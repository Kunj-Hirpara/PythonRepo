# What is an Iterator?
# An iterator is an object that allows you to access elements one at a time.

numbers = [10, 20, 30]
iterator = iter(numbers)
print(next(iterator))
print(next(iterator))
print(next(iterator))